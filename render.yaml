services:
  - type: web
    name: teiki-chosa-input
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start:cloud
    healthCheckPath: /api/health/
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PILOT_PASSWORD
        sync: false
      - key: GOOGLE_SHEETS_SPREADSHEET_ID
        sync: false
      - key: GOOGLE_SERVICE_ACCOUNT_EMAIL
        sync: false
      - key: GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY
        sync: false
      - key: OCR_MODE
        value: economy
      - key: OCR_PROVIDER
        value: paddle
      - key: PADDLE_OCR_ENDPOINT
        sync: false
      - key: PADDLE_OCR_TOKEN
        sync: false
      - key: PADDLE_OCR_TIMEOUT_MS
        value: "30000"
